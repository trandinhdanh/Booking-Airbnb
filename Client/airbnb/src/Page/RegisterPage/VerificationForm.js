import { Form, Input, Button } from 'antd';
import { UserOutlined, LockOutlined } from '@ant-design/icons';

const VerificationForm = () => {
  

  return (
    <div>verify form mail </div>
  );
};

export default VerificationForm;